# Prompt:
Configure ethernet-1/1 with IP address 10.0.0.1/30
and description "Link to core-router" 

# Model: gemma3:4b

# Response:
Okay, here’s the SR Linux configuration to achieve your goal of setting the IP address and description for interface ethernet-1/1, based on the provided documentation.

```
# info with-context network-instance black
network-instance black {
    type ip-vrf
    admin-state enable
    description "Sample network instance"
    router-id 192.168.2.1
    ip-forwarding {
        receive-ipv4-check true
    }
    interface ethernet-1/1.1 {
    }
    interface ethernet-1/2.1 {
    }
    interface lo0.1 {
    }
    protocols {
        linux {
            export-routes true
        }
    }
}

# info with-context network-instance default
network-instance default {
    type ip-vrf
    admin-state enable
    description "Sample network instance"
    router-id 192.168.2.1
    ip-forwarding {
        receive-ipv4-check true
    }
    interface ethernet-1/1.1 {
        description "Link to core-router"
        ip address 10.0.0.1/30
    }
    interface ethernet-1/2.1 {
    }
    interface lo0.1 {
    }
    protocols {
        linux {
            export-routes true
        }
    }
}
```

**Explanation:**

1.  **`network-instance default`**: We’re configuring within the `default` network instance, as that's the context of the provided documentation.
2.  **`interface ethernet-1/1.1`**:  This specifies the interface we are configuring – `ethernet-1/1.1`.
3.  **`description "Link to core-router"`**:  This sets the description for the interface to "Link to core-router," as requested.
4.  **`ip address 10.0.0.1/30`**: This command assigns the IP address `10.0.0.1` with a subnet mask of `/30` (equivalent to 255.255.255.252) to the `ethernet-1/1.1` interface.

**Important Considerations and Best Practices (Based on Documentation):**

*   **Network Instance Context:**  The configuration is done within the `default` network instance.
*   **DHCP vs. Static:** This configuration uses a *static* IP address.  If you want the router to obtain an IP address automatically, you'd need to use DHCP instead, but that's not what the user requested.
*   **Prefix Length:**  The `/30` prefix length is crucial. It defines the size of the subnet.  Ensure it aligns with your network design.
*   **No other configuration changes** are needed.

I have based my answer directly on the provided documentation.  Let me know if you have any further questions or would like to modify this configuration!
